<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.moetaz.androidtoturials">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".mainpackage.AppControl"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainpackage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".retrofitToturial.MainActivityRetrofit" />
        <activity android:name=".simpedagger2example.Main3Activity" />
        <activity android:name=".dagger2retrofitokhttpgson.Main2Activity" />
        <activity android:name=".broadcastrecieverexample.Main4Activity" />

        <receiver android:name=".broadcastrecieverexample.NumberReciever">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE"></action>
            </intent-filter>
        </receiver>

        <activity android:name=".simpleBR.Main5Activity"></activity>
    </application>

</manifest>